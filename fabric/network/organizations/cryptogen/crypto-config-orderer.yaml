# Orderer crypto configuration — single orderer node.
# Replaces fabric-samples/test-network/organizations/cryptogen/crypto-config-orderer.yaml.
# Simplified: only 1 orderer (not 4) since we run a single-node Raft cluster.

OrdererOrgs:
  - Name: Orderer
    Domain: example.com
    EnableNodeOUs: true
    Specs:
      - Hostname: orderer
        SANS:
          - localhost
